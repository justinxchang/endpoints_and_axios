import React from 'react';
import User from './User';

export default function UsersList( { usersList, handleDelete } ) {
  
//  const user = usersList.map( (user, i) => {
//   return <User user={user} 
//                handleDelete={ handleDelete } 
//                key={ 'User'+i } />
//  } )
  return (
    <section className="users">
      {/* { user } */}
    </section>
  );
}